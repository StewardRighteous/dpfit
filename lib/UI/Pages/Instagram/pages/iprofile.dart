import 'package:flutter/material.dart';

class IProfile extends StatelessWidget {
  const IProfile({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}

import 'package:flutter/material.dart';

class IActivity extends StatelessWidget {
  const IActivity({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
